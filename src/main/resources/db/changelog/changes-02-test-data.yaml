databaseChangeLog:
  - changeSet:
      id: seed-data
      author: aaaaaaa
      changes:
          - customChange:
              class: dev.delivery.liquibase.InitializerChange
      rollback:
          - sql:
              sql: |
                DELETE FROM performers;
                DELETE FROM tags;
                DELETE FROM products;